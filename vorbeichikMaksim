#include <stdio.h>
#include <stdlib.h>

struct zzz {
int x;
char rr[255];
float y;
};
                     int main(int argc, char *argv[])
{
                     struct zzz p1;
                     FILE *fp;
                      int raz;
                      if ((argc == 1) || (!(fopen(argv[1], "rb")))) {


                     printf("fael: %s name\n", argv[0]);
                      return 1;
                         }
                         /*/
                         *input = fopen(strFile, "rb");
if (*input == NULL)
{
printf("\n");
File[0] = 0;
}
else
{
printf(strFile);
/*/

fp = fopen(argv[1], "rb");
while (fread(&p1, sizeof(struct zzz), 1, fp)) {
printf("%d,%s,%f\n", p1.x, p1.rr, p1.y);
}
fclose(fp);
raz = ftell(fp);
printf("-----------------------------");
printf("Size of file: %d", raz);

return 0;
}
